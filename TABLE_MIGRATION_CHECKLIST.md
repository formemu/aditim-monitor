# ✅ ЧЕКЛИСТ МИГРАЦИИ ТАБЛИЦ НА BaseTable

## 📊 ОБЩИЙ ПРОГРЕСС

**Мигрировано:** 1 / ~20 методов
**Сэкономлено:** 8 строк
**Статус:** � В процессе

---

## 🪟 WINDOW_PROFILE.PY (1 таблица)

- [x] `update_profile_table()` — Таблица профилей ✅
  - **Строк до:** 18
  - **Строк после:** 10
  - **Экономия:** -8
  - **Также исправлено:** `on_main_table_clicked()` использует `BaseTable.get_selected_id()`

**Итого: 1/1 | -8 строк** ✅

---

## 🔧 WINDOW_MACHINE.PY (0 таблиц)

Нет таблиц для миграции

---

## ⚙️ WINDOW_SETTING.PY (3 таблицы)

- [ ] `update_table_dimension()` — Таблица размерностей
  - **Строк до:** 14
  - **Ожидаемо после:** ~11
  - **Экономия:** -3

- [ ] `update_table_component_type()` — Таблица типов компонентов
  - **Строк до:** ~18
  - **Ожидаемо после:** ~12
  - **Экономия:** -6

- [ ] `update_table_plan_stage()` — Таблица этапов плана
  - **Строк до:** ~16
  - **Ожидаемо после:** ~11
  - **Экономия:** -5

**Итого: 0/3 | -0 строк**

---

## 📋 WINDOW_TASK.PY (5 таблиц)

- [ ] `update_table_task()` — Таблица задач
  - **Строк до:** ~20
  - **Ожидаемо после:** ~12
  - **Экономия:** -8

- [ ] `update_table_queue()` — Таблица очереди
  - **Строк до:** ~18
  - **Ожидаемо после:** ~12
  - **Экономия:** -6

- [ ] `update_queue_component_table()` — Таблица компонентов очереди
  - **Строк до:** ~15
  - **Ожидаемо после:** ~12
  - **Экономия:** -3

- [ ] `update_task_component_table()` — Таблица компонентов задачи
  - **Строк до:** ~25
  - **Ожидаемо после:** ~18
  - **Экономия:** -7

- [ ] `update_table_component_stage()` — Таблица этапов компонента
  - **Строк до:** ~18
  - **Ожидаемо после:** ~12
  - **Экономия:** -6

**Итого: 0/5 | -0 строк**

---

## 📦 WINDOW_BLANK.PY (2 таблицы)

- [ ] `update_table_blank()` — Таблица заказов заготовок
  - **Строк до:** ~70 (сложная, с группировкой)
  - **Ожидаемо после:** ~55
  - **Экономия:** -15
  - **⚠️ Низкий приоритет** — сложная логика

- [ ] `update_table_stock()` — Таблица остатков
  - **Строк до:** ~18
  - **Ожидаемо после:** ~12
  - **Экономия:** -6

**Итого: 0/2 | -0 строк**

---

## 🛠️ WINDOW_DEVELOPMENT.PY (2 таблицы)

- [ ] `update_table_task_dev()` — Таблица задач разработки
  - **Строк до:** ~15
  - **Ожидаемо после:** ~10
  - **Экономия:** -5

- [ ] `update_table_task_component()` — Таблица компонентов задачи
  - **Строк до:** ~18
  - **Ожидаемо после:** ~13
  - **Экономия:** -5

**Итого: 0/2 | -0 строк**

---

## 🏭 WINDOW_PRODUCT.PY (4 таблицы)

- [ ] `update_table_profiletool()` — Таблица инструментов профиля
  - **Строк до:** 16
  - **Ожидаемо после:** ~11
  - **Экономия:** -5

- [ ] `update_table_product()` — Таблица изделий
  - **Строк до:** 16
  - **Ожидаемо после:** ~11
  - **Экономия:** -5

- [ ] `update_profiletool_component_table()` — Таблица компонентов инструмента
  - **Строк до:** ~20
  - **Ожидаемо после:** ~14
  - **Экономия:** -6

- [ ] `update_product_component_table()` — Таблица компонентов изделия
  - **Строк до:** ~16
  - **Ожидаемо после:** ~11
  - **Экономия:** -5

- [ ] `update_table_component_history()` — Таблица истории компонента
  - **Строк до:** ~15
  - **Ожидаемо после:** ~12
  - **Экономия:** -3

**Итого: 0/5 | -0 строк**

---

## 📊 ОБЩАЯ СТАТИСТИКА

### По приоритету:

#### 🟢 Высокий приоритет (7 таблиц):
- window_profile.py: `update_profile_table()` ✅ НАЧАТЬ ОТСЮДА
- window_setting.py: `update_table_dimension()`
- window_setting.py: `update_table_component_type()`
- window_product.py: `update_table_profiletool()`
- window_product.py: `update_table_product()`
- window_development.py: `update_table_task_dev()`
- window_development.py: `update_table_task_component()`

**Ожидаемая экономия: ~35-40 строк**

#### 🟡 Средний приоритет (9 таблиц):
- window_setting.py: `update_table_plan_stage()`
- window_task.py: `update_table_task()`
- window_task.py: `update_table_queue()`
- window_task.py: `update_queue_component_table()`
- window_task.py: `update_task_component_table()`
- window_task.py: `update_table_component_stage()`
- window_product.py: `update_profiletool_component_table()`
- window_product.py: `update_product_component_table()`
- window_product.py: `update_table_component_history()`

**Ожидаемая экономия: ~45-50 строк**

#### 🔴 Низкий приоритет (2 таблицы):
- window_blank.py: `update_table_blank()` — сложная группировка
- window_blank.py: `update_table_stock()`

**Ожидаемая экономия: ~20-25 строк**

---

## 🎯 ИТОГОВАЯ ЦЕЛЬ

- **Всего таблиц:** ~20
- **Ожидаемая экономия:** 100-130 строк
- **Среднее сокращение:** 25-35% кода таблиц
- **Время миграции:** ~2-3 часа для всех файлов

---

## 📝 ИНСТРУКЦИИ

### Для каждой миграции:

1. **Открыть файл** и найти метод `update_*_table()`
2. **Добавить импорт:**
   ```python
   from ..base_table import BaseTable
   ```
3. **Заменить код** на вызов `BaseTable.populate_table()`
4. **Протестировать** — запустить клиент и проверить таблицу
5. **Отметить в чеклисте** — поставить ✅

### Шаблон замены:

**Было:**
```python
def update_profile_table(self):
    table = self.ui.tableWidget_profile
    table.setRowCount(len(data))
    table.setColumnCount(2)
    table.setHorizontalHeaderLabels(["Col1", "Col2"])
    table.horizontalHeader().setStretchLastSection(True)
    
    for row, item in enumerate(data):
        item1 = QTableWidgetItem(item['field1'])
        item2 = QTableWidgetItem(item['field2'])
        item1.setData(Qt.UserRole, item['id'])
        item2.setData(Qt.UserRole, item['id'])
        table.setItem(row, 0, item1)
        table.setItem(row, 1, item2)
```

**Стало:**
```python
def update_profile_table(self):
    BaseTable.populate_table(
        self.ui.tableWidget_profile,
        ["Col1", "Col2"],
        data,
        func_row_mapper=lambda x: [x['field1'], x['field2']],
        func_id_getter=lambda x: x['id']
    )
```

---

**🚀 НАЧИНАЙ С ПРОСТЫХ ТАБЛИЦ (window_profile.py, window_product.py)!**
