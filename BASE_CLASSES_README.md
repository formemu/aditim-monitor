# 🎯 Базовые классы для ADITIM Monitor

## Что это?

Система базовых классов для стандартизации всех окон и диалогов в клиенте ADITIM Monitor.

**Цель:** Устранить дублирование кода и обеспечить единообразие в загрузке UI.

---

## 📦 Состав

### Базовые классы
- **`base_window.py`** — для главных окон (windows/*.py)
- **`base_dialog.py`** — для диалогов (widgets/**/dialog_*.py)

### Примеры миграции
- **`window_profile.py`** — мигрировано ✅
- **`dialog_create_profile.py`** — мигрировано ✅

### Документация
- **`BASE_CLASSES_DOCS.md`** — подробная документация по API
- **`MIGRATION_GUIDE.md`** — пошаговая инструкция по миграции
- **`MIGRATION_CHECKLIST.md`** — чек-лист прогресса миграции

---

## ✨ Преимущества

### До
```python
class WindowProfile(QWidget):
    def __init__(self):
        super().__init__()
        self.profile = None
        self.load_ui()
        self.setup_ui()
        api_manager.data_updated.connect(self.refresh_data)
    
    def load_ui(self):
        """17 строк кода"""
        ui_file = QFile(UI_PATHS_ABS["PROFILE_CONTENT"])
        ui_file.open(QFile.ReadOnly)
        loader = QUiLoader()
        self.ui = loader.load(ui_file)
        ui_file.close()
    
    def load_logo(self):
        """12 строк кода"""
        logo_path = ICON_PATHS_ABS.get("ADITIM_LOGO_MAIN")
        pixmap = QPixmap(logo_path)
        scaled = pixmap.scaled(300, 100, Qt.KeepAspectRatio)
        self.ui.label_logo.setPixmap(scaled)
```

### После
```python
class WindowProfile(BaseWindow):
    def __init__(self):
        self.profile = None
        super().__init__(UI_PATHS_ABS["PROFILE_CONTENT"], api_manager)
    
    def setup_ui(self):
        self.apply_styles()
        self.load_logo()
        # Только специфичная логика
```

**Экономия:** -31 строка на каждое окно!

---

## 🚀 Быстрый старт

### 1. Прочитай документацию
```
📖 BASE_CLASSES_DOCS.md — что такое BaseWindow и BaseDialog
📋 MIGRATION_GUIDE.md — как мигрировать файлы
```

### 2. Посмотри примеры
```
✅ window_profile.py — пример окна
✅ dialog_create_profile.py — пример диалога
```

### 3. Выбери файл для миграции
```
📝 MIGRATION_CHECKLIST.md — список всех файлов
```

### 4. Мигрируй файл
Следуй инструкциям из MIGRATION_GUIDE.md

### 5. Тестируй
```bash
# Проверь ошибки
get_errors для файла

# Запусти клиент
Run Client task
```

---

## 📊 Статус

**Прогресс:** 2/27 файлов (7%)

### Windows: 1/7 ✅
- [x] window_profile.py

### Dialogs: 1/20+ ✅
- [x] dialog_create_profile.py

### Ожидаемая экономия кода: ~500 строк

---

## 🎯 Следующие шаги

1. **Мигрируй простые окна:**
   - window_machine.py
   - window_setting.py
   - window_task.py

2. **Затем простые диалоги:**
   - dialog_edit_profile.py
   - dialog_create_product.py
   - dialog_edit_product.py

3. **Проверяй каждый файл:**
   - get_errors
   - Запуск клиента
   - Тестирование функционала

---

## 💡 Важно помнить

- ✅ **Делай только то, что просит пользователь** — не предлагай лишнего
- ✅ **НЕ ТРОГАЙ БАЗУ ДАННЫХ** — все манипуляции с БД делает пользователь
- ✅ **Единственное число** — во всех именах
- ✅ **Порядок "тип → содержимое"** — list_tool, count_deleted
- ✅ **UI-FIRST** — только .ui файлы, никакого программного создания виджетов

---

## 📚 Полезные ссылки

- **Copilot Instructions:** `.github/copilot-instructions.md`
- **UI Files:** `src/client/ui/*.ui`
- **Constants:** `src/client/constant.py`

---

**🤖 Готово к использованию! Начинай миграцию по одному файлу за раз.**
