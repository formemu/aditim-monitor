# ADITIM Monitor

Система управления задачами для цеха металлообработки. Позволяет менеджерам создавать задачи, а рабочим просматривать приоритетную очередь задач.

## 📋 Описание

ADITIM Monitor - это клиент-серверная система управления задачами для металлообрабатывающего цеха. Система отслеживает производственные задачи по созданию профилей/инструментов и других производственных работ.

### Основные возможности

- **Управление задачами**: Создание, редактирование, удаление задач
- **Два типа задач**:
  - Инструменты для экструзии (профили)
  - Другие производственные работы (продукты)
- **Приоритизация**: Управление очередью задач с помощью drag-and-drop
- **Статусы**: Отслеживание статуса выполнения задач
- **Отделы**: Назначение задач по отделам

## 🏗️ Архитектура

### Серверная часть (FastAPI)
- REST API для управления задачами
- SQLite база данных
- SQLAlchemy ORM модели
- Автоматическая документация API

### Клиентская часть (PySide6)
- Современный графический интерфейс
- Асинхронные операции через QThread
- Темная тема оформления
- Drag-and-drop для изменения приоритета

## 🚀 Установка и запуск

### Требования
- Python 3.8+
- pip

### Установка зависимостей
```bash
pip install -r requirements.txt
```

### Запуск сервера
```bash
python run_server.py
```
Сервер запустится на `http://localhost:8000`

### Запуск клиента
```bash
python run_client.py
```

## 📊 База данных

### Основные сущности

**Задачи (Tasks)**
- ID, ID продукта/профиля, оборудование, дедлайн
- Позиция в очереди, тип работы, статус, отдел

**Профили (Profiles)**
- Инструменты для экструзии
- 7 стандартных типов оборудования

**Продукты (Products)**
- Другие производственные работы
- Настраиваемое оборудование

**Справочники**
- Статусы очереди
- Типы работ
- Отделы
- Компоненты

## 🎯 Типы задач

### 1. Инструменты для экструзии
- Фиксированное оборудование: плиты 1-4, пальцы, усреднитель, кондуктор
- Типы работ: новый инструмент, новый вариант, добавить к существующему, переделать, доработка

### 2. Другие работы
- Настраиваемый список оборудования
- Гибкое именование и количество
- Обязательное назначение отдела

## 🔧 API Endpoints

```
GET /api/tasks/                    # Получить задачи
POST /api/tasks/                   # Создать задачу
PUT /api/tasks/{id}               # Обновить задачу
DELETE /api/tasks/{id}            # Удалить задачу
PUT /api/tasks/{id}/position      # Обновить позицию

GET /api/directories/statuses/     # Статусы
GET /api/directories/departments/  # Отделы
GET /api/directories/work_types/   # Типы работ
```

## 💻 Интерфейс пользователя

### Главное окно
- Таблица задач с фильтрацией
- Двойной клик для изменения статуса
- Drag-and-drop для изменения приоритета
- Кнопки создания/удаления задач

### Диалог создания задач
- Вкладка "Профиль" для инструментов экструзии
- Вкладка "Продукт" для других работ
- Выбор оборудования, типа работы, дедлайна

## 🛠️ Разработка

### Структура проекта
```
src/
├── server/              # FastAPI приложение
│   ├── main.py         # Главный файл сервера
│   ├── models.py       # SQLAlchemy модели
│   └── database.py     # Настройка БД
├── client/             # PySide6 приложение
│   ├── main.py         # Главный файл клиента
│   ├── main_window.py  # Главное окно
│   ├── task_dialog.py  # Диалог создания задач
│   ├── api_client.py   # HTTP клиент
│   └── constants.py    # Константы UI
```

### Стиль кода
- Black для форматирования
- Type hints обязательны
- Docstrings для всех классов и методов
- SOLID принципы

## 📝 Лицензия

Этот проект создан для образовательных целей.

## 👥 Авторы

- Разработчик: GitHub Copilot
- Проект: ADITIM Monitor

## 🐛 Сообщить об ошибке

Если вы нашли ошибку, пожалуйста, создайте issue в этом репозитории.
- Отдельные диалоги для создания/редактирования задач

## Типы задач

1. **Инструмент на экструзию (профили)**:
   - Фиксированный набор оборудования: плиты 1-4, пальцы, усреднитель, кондуктор
   - Типы работ: новый инструмент, новый вариант, доработка и т.д.

2. **Другие задачи**:
   - Произвольное оборудование
   - Настраиваемые компоненты

## Установка

### Требования
- Python 3.8+
- pip

### Быстрая установка
```bash
python setup.py
```

### Ручная установка зависимостей
```bash
pip install -r requirements.txt
```

### Проверка установки
После установки зависимостей ошибки импорта в IDE должны исчезнуть.

## Запуск

### Быстрый запуск (рекомендуется)
```bash
# Сервер
python run_server.py

# Клиент (в отдельном терминале)
python run_client.py
```

### Альтернативный запуск
```bash
# Сервер
cd src/server
python main.py

# Клиент  
cd src/client
python main.py
```

## Структура проекта

```
src/
├── server/                  # FastAPI сервер
│   ├── main.py             # Точка входа сервера
│   ├── database.py         # Настройка базы данных
│   ├── models/             # SQLAlchemy модели
│   ├── api/                # API маршруты
│   └── schemas/            # Pydantic схемы
├── client/                 # PySide6 клиент
│   ├── main.py             # Точка входа клиента
│   ├── main_window.py      # Главное окно
│   ├── api_client.py       # HTTP клиент
│   ├── constants.py        # Константы
│   ├── ui/                 # UI файлы
│   ├── widgets/            # Пользовательские виджеты
│   └── resources/          # Ресурсы (стили, иконки)
└── shared/                 # Общие утилиты
```

## API Документация

После запуска сервера документация доступна по адресу:
- Swagger UI: http://localhost:8000/docs
- ReDoc: http://localhost:8000/redoc

### Основные эндпоинты

- `GET /api//tasks/` - Получить задачи с фильтрацией
- `POST /api/tasks/` - Создать задачу
- `PUT /api/tasks/{id}` - Обновить задачу
- `PUT /api/tasks/{id}/position` - Изменить позицию задачи
- `GET /api/directories/statuses/` - Получить статусы
- `GET /api/directories/departments/` - Получить департаменты

## Разработка

### Стиль кода
- Используется black, flake8, mypy
- Соблюдение принципов SOLID, DRY, KISS
- Type hints для всех функций

### Тестирование
```bash
pytest tests/ -v
```

### Линтеры
```bash
black src/
flake8 src/
mypy src/
```

## База данных

SQLite база данных создается автоматически при первом запуске сервера.

Основные таблицы:
- `tasks` - Задачи
- `product` - Продукты
- `profile` - Профили
- `dir_*` - Справочные таблицы

## Лицензия

Внутренний проект компании.
