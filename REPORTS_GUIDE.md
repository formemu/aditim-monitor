# 📊 РУКОВОДСТВО ПО ОКНУ ОТЧЁТОВ

## 🎯 Общая информация

**Окно отчётов** предоставляет аналитические инструменты для анализа производственных данных.

**Статус:** ✅ Базовая версия реализована (13.12.2025)

**Файлы:**
- UI: `src/client/ui/content_report.ui`
- Python: `src/client/windows/window_report.py`
- Константы: `src/client/constant.py` (добавлен `CONTENT_REPORT`)
- Интеграция: `src/client/main_window.py` (метод `show_report()`)

---

## 📋 РЕАЛИЗОВАННЫЕ ОТЧЁТЫ

### 1. ✅ Отчёт по задачам (ГОТОВ)

**Описание:**  
Анализ задач за выбранный период с фильтрацией по отделам, типам и статусам.

**Данные:**
- Таблица: `task`
- Справочники: `dir_task_type`, `dir_department`, `dir_task_status`, `dir_task_location`

**Колонки таблицы:**
1. Номер
2. Тип
3. Отдел
4. Статус
5. Местоположение
6. Создана
7. Срок
8. Описание

**Сводка включает:**
- Общее количество задач
- Распределение по статусам (с процентами)
- Распределение по отделам (с процентами)

**Фильтры:**
- По отделу (`checkBox_filter_department` + `comboBox_department`)
- По типу задачи (`checkBox_filter_task_type` + `comboBox_task_type`)
- По статусу (`checkBox_filter_status` + `comboBox_status`)

**Пример использования:**
```python
# Пользователь выбирает:
# - Тип отчёта: "Отчёт по задачам"
# - Период: Декабрь 2025
# - Фильтр: Отдел "Производство"
# Нажимает "Сформировать отчёт"
# Получает таблицу и сводку
```

---

### 2. ⏳ Отчёт по станкам (В РАЗРАБОТКЕ)

**Планируется:**
- Загрузка станков по времени работы
- Анализ простоев
- Топ станков по количеству операций

**Данные:**
- `task_component_stage`, `dir_machine`, `dir_work_subtype`

---

### 3. ⏳ Отчёт по заготовкам (В РАЗРАБОТКЕ)

**Планируется:**
- Движение заготовок (приход/расход)
- Текущие остатки
- Критические уровни запасов

**Данные:**
- `blank`, `dir_blank_material`, `dir_blank_type`

---

### 4. ⏳ Отчёт по инструментам (В РАЗРАБОТКЕ)

**Планируется:**
- Статус разработки профилеинструментов
- Загрузка по типам компонентов
- Сроки изготовления

**Данные:**
- `profiletool`, `product`, `dir_component_type`

---

### 5. ⏳ Сводный отчёт (В РАЗРАБОТКЕ)

**Планируется:**
- KPI метрики за период
- Общая статистика по всем сущностям
- Графики и диаграммы

---

## 🎨 СТРУКТУРА UI

### Верхняя панель управления (QHBoxLayout)

**1. GroupBox "Тип отчёта"**
- `comboBox_report_type`: Выбор типа отчёта (5 вариантов)

**2. GroupBox "Период"**
- `radioButton_month` (по умолчанию): Режим месяца
- `comboBox_month`: Выбор месяца (1-12)
- `comboBox_year`: Выбор года (2023-2026)
- `radioButton_range`: Режим произвольного диапазона
- `dateEdit_from`: Дата начала
- `dateEdit_to`: Дата окончания

**3. GroupBox "Фильтры"**
- `checkBox_filter_department` + `comboBox_department`
- `checkBox_filter_task_type` + `comboBox_task_type`
- `checkBox_filter_status` + `comboBox_status`

### Кнопки действий (QHBoxLayout)

- `pushButton_generate`: "Сформировать отчёт" (200×40 px)
- `pushButton_export`: "Экспорт в Excel" (150×40 px, изначально disabled)

### Вкладки результатов (QTabWidget)

**Tab 1: "Таблица"**
- `tableWidget_report`: QTableWidget с результатами

**Tab 2: "Сводка"**
- `textEdit_summary`: QTextEdit (read-only) с аналитикой

---

## 🔧 ТЕХНИЧЕСКИЕ ДЕТАЛИ

### Методы класса WindowReport

**Инициализация:**
```python
def __init__(self, api_manager):
    self.api_manager = api_manager
    self.current_report_data = []
    self.load_ui()
    self.setup_ui()
    self.connect_signals()
```

**Основные методы:**
- `get_date_range() -> tuple[date, date]` - Получение диапазона дат
- `get_filters() -> Dict[str, Any]` - Получение активных фильтров
- `on_generate_report()` - Обработка кнопки "Сформировать отчёт"
- `generate_report_task()` - Генерация отчёта по задачам
- `generate_summary_task()` - Формирование текстовой сводки
- `on_export_excel()` - Экспорт в Excel файл

**Фильтрация данных:**
```python
# Фильтр по периоду
task_date = datetime.fromisoformat(task["created"]).date()
if date_from <= task_date <= date_to:
    # Применяем дополнительные фильтры
    if "department_id" in filters:
        if task.get("department_id") != filters["department_id"]:
            continue
    list_filtered.append(task)
```

**Использование BaseTable:**
```python
BaseTable.populate_table(
    self.ui.tableWidget_report,
    list_header,
    list_filtered,
    func_row_mapper=map_row,
    func_id_getter=lambda t: t["id"]
)
```

### Экспорт в Excel

**Зависимость:** `openpyxl` (опциональная)

**Установка:**
```powershell
pip install openpyxl
```

**Функционал:**
- Экспорт таблицы с заголовками
- Автоматическая ширина колонок
- Стилизация заголовков (жирный шрифт, серый фон)
- Имя файла по умолчанию: `report_YYYYMMDD_HHMMSS.xlsx`

**Обработка ошибок:**
- Проверка наличия данных перед экспортом
- Информирование пользователя об отсутствии библиотеки
- Try-except для перехвата ошибок записи

---

## 📊 ПРИМЕРЫ ИСПОЛЬЗОВАНИЯ

### Пример 1: Отчёт за месяц без фильтров

1. Открыть окно отчётов (кнопка "Отчёты" в главном меню)
2. Выбрать "Отчёт по задачам"
3. Выбрать "Месяц" → Декабрь 2025
4. Нажать "Сформировать отчёт"
5. Просмотреть таблицу и сводку

### Пример 2: Фильтрация по отделу

1. Включить `checkBox_filter_department`
2. Выбрать отдел в `comboBox_department`
3. Нажать "Сформировать отчёт"
4. Получить данные только по выбранному отделу

### Пример 3: Экспорт в Excel

1. Сформировать любой отчёт
2. Нажать "Экспорт в Excel"
3. Выбрать путь сохранения
4. Получить .xlsx файл с данными

### Пример 4: Произвольный диапазон дат

1. Выбрать "Произвольный диапазон"
2. Установить дату от: 01.11.2025
3. Установить дату до: 30.11.2025
4. Нажать "Сформировать отчёт"

---

## 🎯 ПЛАНЫ РАЗВИТИЯ

### Ближайшие задачи

**Приоритет 1: Доработка отчёта по задачам**
- ✅ Базовая таблица и сводка
- ⏳ Добавить сортировку колонок
- ⏳ Добавить поиск в таблице
- ⏳ Графики (pie chart по статусам, bar chart по отделам)

**Приоритет 2: Отчёт по станкам**
- ⏳ Реализовать базовую таблицу
- ⏳ Расчёт загрузки (часы работы / общее время)
- ⏳ Топ-5 самых загруженных станков
- ⏳ График загрузки по дням

**Приоритет 3: Отчёт по заготовкам**
- ⏳ Таблица остатков
- ⏳ Индикаторы критических уровней
- ⏳ История движения
- ⏳ Прогноз расхода

### Долгосрочные улучшения

- 📈 Интеграция графиков (matplotlib или Chart.js)
- 📧 Автоматическая отправка отчётов по email
- 📅 Планировщик отчётов (ежедневные/еженедельные)
- 💾 Сохранение шаблонов отчётов
- 🔄 Сравнение периодов (месяц к месяцу)
- 📊 Dashboard с KPI метриками

---

## 🐛 ИЗВЕСТНЫЕ ПРОБЛЕМЫ

**1. Отсутствие openpyxl**
- **Симптом:** При попытке экспорта показывается предупреждение
- **Решение:** `pip install openpyxl`

**2. Пустые справочники при старте**
- **Симптом:** Фильтры пустые, если сервер не запущен
- **Решение:** Запустить API сервер перед открытием окна отчётов

**3. Медленная генерация больших отчётов**
- **Симптом:** Зависание UI при обработке >1000 задач
- **Решение:** Планируется добавить асинхронную обработку

---

## 📝 СОБЛЮДЕНИЕ СТАНДАРТОВ ПРОЕКТА

### ✅ Единственное число
- Все названия: `report`, `task`, `component` (не множественное число)

### ✅ Порядок "тип → содержимое"
- `comboBox_report_type` (не `report_type_comboBox`)
- `list_task` (не `task_list`)

### ✅ UI-FIRST
- Все элементы созданы в `content_report.ui`
- Никакого программного создания виджетов
- Прямой доступ через `self.ui.pushButton_generate`

### ✅ Использование BaseTable
- `BaseTable.populate_table()` для заполнения таблицы
- `func_row_mapper` для преобразования данных
- `func_id_getter` для сохранения ID в UserRole

### ✅ Константы
- `UI_PATHS["CONTENT_REPORT"]` добавлен в `constant.py`
- Все пути через константы

---

## 🚀 БЫСТРЫЙ СТАРТ

**1. Запустить сервер:**
```powershell
.venv\Scripts\python.exe run_server.py
```

**2. Запустить клиент:**
```powershell
.venv\Scripts\python.exe run_client.py
```

**3. Открыть окно отчётов:**
- Нажать кнопку "Отчёты" в главном меню

**4. Сформировать первый отчёт:**
- Выбрать "Отчёт по задачам"
- Выбрать период
- Нажать "Сформировать отчёт"

**5. (Опционально) Экспорт:**
- Установить openpyxl: `pip install openpyxl`
- Нажать "Экспорт в Excel"

---

**🎉 Окно отчётов готово к использованию!**
